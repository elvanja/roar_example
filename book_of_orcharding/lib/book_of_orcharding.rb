require "book_of_orcharding/version"

# https://github.com/apotonick/roar-rails#url-helpers
# don't require here, or url helpers won't work
#root = File.expand_path('..', __FILE__)
#%w{representers}.each do |dir|
  #Dir["#{root}/book_of_orcharding/#{dir}/**/*.rb"].sort.each { |source| require source }
#end

module FruitOrcharding
end
